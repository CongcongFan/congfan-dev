{
  "name": "congfan-dev-pages",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio and AI product operator landing page for congfan.dev.",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "lint": "node ./scripts/lint.mjs",
    "test": "node ./scripts/test.mjs",
    "deploy": "wrangler pages deploy dist --project-name=congfan-dev",
    "deploy:brief": "wrangler deploy --config wrangler.brief.toml",
    "deploy:all": "npm run build && npm run deploy && npm run deploy:brief"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
